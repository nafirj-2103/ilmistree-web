Drop your 3D model files here for use by the frontend.

For the course-card book example the file should be named `book.glb` and
placed at the root of this folder.  Vite will serve it at `/models/book.glb`.

The code relies on that exact path, so adjust imports if necessary.